## Summary

<one or two sentences: what changed and why>

## Acceptance

- [ ] Tests pass: `python3 -m unittest discover -s tests`
- [ ] Skill validator passes: `python3 scripts/validate_skill.py work-queue`
- [ ] Queue validator passes on bundled fixtures
- [ ] `CHANGELOG.md` updated under `[Unreleased]` if user-visible
- [ ] No new third-party dependencies (or recorded decision)

## Outcome

<changed files, screenshot if relevant, before/after for behavior
changes>
